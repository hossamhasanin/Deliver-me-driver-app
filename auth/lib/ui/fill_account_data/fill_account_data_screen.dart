import 'package:auth/ui/fill_account_data/components/body.dart';
import 'package:flutter/material.dart';

class FillAccountDataScreen extends StatelessWidget {
  const FillAccountDataScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Body(),
    );
  }
}
